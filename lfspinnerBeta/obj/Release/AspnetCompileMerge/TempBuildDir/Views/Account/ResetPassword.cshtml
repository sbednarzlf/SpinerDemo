@model lfspinnerBeta.Models.ResetPasswordViewModel
@{
    ViewBag.Title = "Reset password";
}

<h2>@ViewBag.Title.</h2>
<div class="vertical-registration-form">
    @using (Html.BeginForm("ResetPassword", "Account", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
    {
        @Html.AntiForgeryToken()
        <h4>Zmiana chasła.</h4>
        <hr />
        @Html.ValidationSummary("", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.Code)
       
               @Html.TextBoxFor(m => m.Email, new { type = "email", @class = "form-control input-box", placeholder = "Email" })


               @Html.PasswordFor(m => m.Password, new { type = "password", id = "cf-password", name = "password", @class = "form-control input-box", placeholder = "Hasło" })

               @Html.PasswordFor(m => m.Password, new { type = "password", id = "cf-password", name = "password", @class = "form-control input-box", placeholder = "Hasło" })

              <button class="btn btn-primary standard-button">Reset</button>
    }
    </div>


    @section Scripts {
        @Scripts.Render("~/bundles/jqueryval")
    }
